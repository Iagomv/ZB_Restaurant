const mongoClient = require('../Connection/mongoConnection') // Importar la conexión a la base de datos

const BD = 'karbo_iMartinez'
const coleccion = 'pedidos'

const obtenerPedidos = async (req, res) => {
  try {
  } catch (error) {}
}
